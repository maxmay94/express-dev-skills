<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheets/style.css">
  <title>Express Dev Skills</title>
</head>
<body>
  <h1>Skills! as of <%= time %> </h1>
  <% if(error) { %> 
    <h2><%= error %> </h2>
  <% } %> 
  <ul>
    <% skills.forEach(skill => { %>
     <li>
       <form action="/skills/<%= skill._id %>?_method=DELETE" class="delete-form" method="POST">
        <button type="submit">X</button>
       </form>
       <a href="/skills/<%= skill._id %>"> <%= skill.text %> </a>
       - 
       <%= skill.hasSkill ? "You DO have this skill, good-onya mate" : "You do NOT have this skill, best get to work son..." %> 
     </li>
    <% }) %>
  </ul>
  <a href="/skills/new">Add New Skill</a>
</body>
</html>